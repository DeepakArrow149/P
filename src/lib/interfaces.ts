// src/lib/interfaces.ts
import { LineGroup, CreateLineGroupData, UpdateLineGroupData, LineGroupFilters } from './lineGroupRepository';
import { LineCapacity, CreateLineCapacityData, UpdateLineCapacityData, LineCapacityFilters } from './lineCapacityRepository';

// Export interfaces for consistent usage across the application
export type {
  LineGroup,
  CreateLineGroupData,
  UpdateLineGroupData,
  LineGroupFilters,
  LineCapacity,
  CreateLineCapacityData,
  UpdateLineCapacityData,
  LineCapacityFilters
};
